'use strict';

describe('myApp.TaskTracker module', function() {

  beforeEach(module('myApp.TaskTracker'));

  describe('TaskTracker controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var TaskTrackerCtrl = $controller('TaskTrackerCtrl');
      expect(TaskTrackerCtrl).toBeDefined();
    }));

  });
});